openapi: 3.0.0
info:
  title: Auth Service
  version: 1.0.0
paths:
  /user:
    get:
      summary: Get user info
      operationId: getUserInfo
      responses:
        '200':
          description: User information
          content:
            application/json:
              schema:
                  $ref: '../schemas/user.yaml#/components/schemas/UserInfo'